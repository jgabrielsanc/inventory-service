spring:
  application:
    name: inventory-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/inventorydb
      auto-index-creation: true

  jackson:
    serialization:
      INDENT_OUTPUT: true

server:
  port: 8080
